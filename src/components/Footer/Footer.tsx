import React from 'react';
import {Box} from "@mui/material";

const Footer = () => {
    return (
        <Box sx={{background:"colors.primary",color:"#fff",
            textAlign:'center',p:2}}>
            Created by vanaf25
        </Box>
    );
};

export default Footer;
